{
  "name": "watchio",
  "version": "1.0.0",
  "private": true,
  "description": "Watchio — synchronized watch party platform",
  "workspaces": [
    "server",
    "web",
    "extension"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:web": "npm run dev --workspace=web",
    "build:extension": "npm run build --workspace=extension"
  }
}
